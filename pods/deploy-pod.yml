apiVersion: v1
kind: Pod
spec:
  containers:
    - name: ansible
      image: docker.io/<DOCKER ACCOUNT>/bootcamp-ansible:latest
      command: ["tail", "-f", "/dev/null"]
      imagePullPolicy: IfNotPresent
